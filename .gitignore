__pycache__/
.bob/
.DS_Store
.env
.env.tokens
.tokmint.db
*.db
*.pyc
node_modules/
target/

# dedicated venv for the Bob mcpgateway.wrapper bridge (local, per-machine)
.wrapper-venv/

# PowerPoint lock/owner files
slides/~$*
docs/assets/cockpit-token.js

# NOTE: Stage 1 has Bob WRITE mcp-servers/sales-tax/server.py live. Do NOT gitignore
# it — Bob (IBM Bob Shell) honors .gitignore and will refuse to create an ignored
# path. The file is generated (not committed); `make stage-reset` removes it so the
# beat repeats clean, and `_solution.py` is the tracked fallback.
